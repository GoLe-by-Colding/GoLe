import { Screen, Text } from "@/shared/ui";

/** 판매 탭. 웹 `views/sell`에 대응한다. (스펙 6단계에서 실제 화면으로 교체) */
export default function SellScreen() {
  return (
    <Screen>
      <Text variant="title">판매</Text>
      <Text muted>준비 중입니다.</Text>
    </Screen>
  );
}
