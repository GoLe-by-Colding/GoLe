import { Screen, Text } from "@/shared/ui";

/** 검색 탭. 웹 `views/search`에 대응한다. (스펙 6단계에서 실제 화면으로 교체) */
export default function SearchScreen() {
  return (
    <Screen>
      <Text variant="title">검색</Text>
      <Text muted>준비 중입니다.</Text>
    </Screen>
  );
}
