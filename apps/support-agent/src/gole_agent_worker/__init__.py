"""내부 영속 작업 실행기. 사용자 권한과 quota는 Java가 소유한다.

**보류 중이다 — 2026-09-22 기준 이 패키지를 쓰는 소비자가 0개다.**

지우지 않은 이유와 켜지 않은 이유가 둘 다 있어서 이 상태로 둔다. 새로 읽는 사람이
"왜 멀쩡해 보이는데 아무도 안 쓰지"에서 멈추지 않도록 여기 적어 둔다.

고유하게 제공하는 것 (이 저장소 어디에도 중복이 없다)
  - ``session.FencedSaver`` — lease 검사와 fencing 토큰 증가를 LangGraph 체크포인트 쓰기와
    **같은 SQLite 트랜잭션**에 묶어, lease 를 잃은 워커의 늦은 완료·늦은 체크포인트를 막는다.
  - 접수 시각에 DB 로 못박는 총 기한 — 재시작·설정 변경으로 연장되지 않고, 실행 워커가
    하나도 없어도 ``get``·중복 ``submit``·``claim`` 세 경로에서 만료가 확정된다.
  - 원문 없는 tombstone 기반 논리 Purge — Java 의 Mongo 파기와 2단으로 엮여 원격 실패 시
    롤백을 가능하게 하고, 파기 뒤 지연 도착한 Submit·완료·체크포인트를 영구히 차단한다.

왜 안 쓰이나 — 후보 셋이 모두 명시적으로 편입을 거부했다
  - 문의: ``agents.support.SupportAgent`` 는 ``analyze_support`` 한 줄 위임이라 동기 서버
    (:50051, 운영 배포됨)와 같은 로직이다. 규칙 분류에 중간 저장이 필요 없다.
  - 사진: ``.kiro/specs/agent-worker/brick-grpc.md`` — "원본 이미지를 SQLite에 넣어
    재시도하는 설계는 채택하지 않는다". 체크포인트 자체가 프라이버시 규율과 충돌한다.
  - 홍보: ``.kiro/specs/promotion-review/spec.md`` — "필요한 것은 영속성이지 분산 job
    leasing 이 아니다". 일회성 프로세스 하나뿐이라 경쟁이 없어 fencing 이 풀 문제가 없다.

깨우는 조건 (새로 만든 것이 아니라 위 문서들이 이미 남긴 트리거다)
  1. 홍보 트리거가 timer 발에서 **관리자 콘솔 발 요청**으로 바뀔 때. 사람이 누르는 순간
     동시 요청·재시도·소유권이 생긴다 (``promotion-review/spec.md`` 의 재검토 조건).
  2. **brickfilter 3회/일 ledger 를 영속 작업과 연결**할 때
     (``apps/support-agent/README.md`` 가 "후속 작업"으로 남긴 유일한 연결 계획).
  3. 둘 다 오지 않은 채 결정 기한이 지나면 제거를 재검토한다. 기한과 근거는 볼트
     ``08_개선과제/알려진 개선 과제.md`` 에 있다.

배포 상태
  운영 이미지(``apps/support-agent/Dockerfile``)는 이 패키지를 **싣지 않는다.** 돌지 않는
  코드가 운영 컨테이너의 표면을 넓히지 않도록 뺐다. 소스·테스트·스펙은 그대로 두므로,
  깨울 때는 Dockerfile 의 ``COPY`` 와 배선만 되돌리면 된다.
  테스트는 계속 CI 에서 전부 돈다 — 보류는 "검증을 멈춘다"는 뜻이 아니다.
"""
