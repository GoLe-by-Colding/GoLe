import { BrickFilterPage } from "@views/brick-filter";
export const metadata = { title: "브릭 필터", robots: { index: false, follow: false } };
export default function Page() {
  return <BrickFilterPage />;
}
