# 검증 기록 — 2026-09-24

- Python 전체: 157 passed. 추가 회귀 5건은 수정 전 실패하고 수정 후 통과함.
- Java `spotlessCheck` 통과, 전체 단위 1,221건 중 1,219 통과·2 스킵, 전체 통합 118건 중 117 통과·1 스킵. `--rerun-tasks`로 실제 실행함.
- 단위 스킵은 실제 HEIF 디코더 2건, 통합 스킵은 실제 알림톡 발송 1건임.
- 실제 MongoDB 경쟁 재제출과 제출 멱등 통합 회귀 2건 통과함.
- 프론트 format → lint → typecheck → fsd:lint → build 통과함. 실행 중인 dev 서버와 별도 dist 디렉터리에서 빌드함.
- 관리자 브라우저 회귀: 30 passed / 5 skipped. 실제 백엔드 관리자 가드 5건은 해당 실행에 관리자 E2E 환경을 주입하지 않아 스킵됨.
- 인증·복귀 경로 회귀: 43 passed / 0 skipped. 실제 카카오 활성화 시 일반 로그인 버튼과 소셜 로그인 버튼이 겹치던 테스트 선택자를 정확히 한정함.

## 확인 범위

유료 외부 모델·Threads 실제 발행·모바일 실기기는 실행하지 않음. 홍보 발행 어댑터는 여전히 스텁이며 화면에도 모의 발행으로 명시함. 로컬 support gRPC는 rules-v1로 실행함. 전체 백엔드 결합 E2E를 이번 로컬 실행에서 재수행하지 않았으므로 CI 결과와 구분함.

팀 기록은 별도 GoLe-obsidian의 `06_개발로그/2026-09-24_에이전트 복구와 카카오 실연동을 보강함.md`에 남김.
